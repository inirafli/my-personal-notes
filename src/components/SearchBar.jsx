import React from 'react'

const SearchBar = () => (
  <section className="search-bar">
    <input type="text" placeholder="Cari Catatan berdasarkan Judul" />
  </section>
)

export default SearchBar
